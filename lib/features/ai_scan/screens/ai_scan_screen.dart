import 'package:flutter/material.dart';

class AiScanScreen extends StatelessWidget {
  const AiScanScreen({super.key});

  @override
  Widget build(BuildContext context) {

    return const Scaffold(

      body: Center(
        child: Text("AI Scan Screen"),
      ),
    );
  }
}